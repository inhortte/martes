#headings
  .heading
    Name
  .heading
    Type
  %br.clear
#entries
  - @mustelids.each do |m|
    .entry
      =h m.name
    .entry
      %em
        = m.species.genus.name
        = m.species.name
    %br.clear
